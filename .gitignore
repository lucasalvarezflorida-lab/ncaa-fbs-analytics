# data caches (regenerable via refresh_all.py)
rosters/data/
fpi-decomposition/data/

# generated output (kept locally; charts regenerable)
fpi-decomposition/output/*
!fpi-decomposition/output/residuals_2025.png
!fpi-decomposition/output/residuals_2026.png
film_study/

# live nflverse download (refetched every nfl_watchlist.py run)
market-postmortem/nfl_games_live.csv
market-postmortem/nfl_watchlist_run.log

# workbook backups, locks, logs
*.bak*
.~lock.*
refresh_log.txt

# Drive API credentials (push_deck.py) - NEVER commit (public repo)
drive_oauth_client.json
drive_token.json

# Circa opening-board screenshots - local proof archive, not for redistribution
circa_openers/

# python
__pycache__/
*.pyc
.pytest_cache/
.env

# OurLads curated data - personal use only, not for redistribution
ourlads_depth.json

# conference decks embed OurLads depth charts - local use only, never commit
decks/2026_*_Projections.pptx

# ESPN CDN logo pulls (re-download via URLs in rosters/data/teams_fbs_2026.json)
decks/logos/

# other people's decks and merges built from them (Corey's content stays out of the repo)
decks/corey_*_export.pptx
decks/*_merged.pptx
decks/*_merged_*.pptx
decks/*_export.pptx

# internal research (market deep dives) - the repo is public, the market is ours
internal/

# every read-only export of Corey's deck stays local
decks/corey_*.pptx
