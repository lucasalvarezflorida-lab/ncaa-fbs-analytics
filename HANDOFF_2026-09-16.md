# Handoff prompt — Man vs Machine, Ep5 / Week 4 (written Wed Sep 16, 2026)

Paste everything below the line into a new session.

---

Continue my "Man vs Machine — College Football Podcast" work (co-host Corey
Reddy, credds19@gmail.com). Project: `C:\Users\lucas\Fun Projects\Sports Data
Analysis\ncaa-fbs-model` (git repo lucasalvarezflorida-lab/ncaa-fbs-analytics,
`git push` is allowed in settings). Read your memory files
`podcast-deck-vs-notes.md`, `corey-slides-rule.md` and
`fpi-decomposition-project.md` first, plus the `cfb-pipeline` skill, then
`HANDOFF_2026-09-13.md` (its dated update blocks) and this file. Last commit:
ad3a0fb.

**Where we are (Wed 9/16):** Episode 4 / Week 3 RECORDED Tue 9/15. Next is
Ep5 / Week 4 (games Sat 9/26; record ~Tue/Wed 9/22–23). Week 3 games are
Fri 9/18–Sat 9/19; grade them Sunday.

**Frozen for Week 3 grading:** machine lines TTU −13, LOU −1, SC −5, AUB
−0.5, LSU −5 (margin-cap rule); machine score calls Tech 33–20, Louisville
30–29, SC 32–27, Auburn 27–26, LSU 32–27; superdogs Sacramento State +27.5
vs NDSU, Utah State +28.5 at Utah (our rule: win only, points = spread —
0-for-4 so far). Corey's calls read off his score slides as home-team-on-top:
Houston 35–24, Louisville 35–34, SC 31–27, Auburn 28–27, LSU 34–28 — CONFIRM
with him; also confirm his Week 2 Michigan "man off by" (11 vs 3). Corey's
superdogs: FIU +7 at FAU, Colorado State +18 vs BYU; his scoring is 5 per
cover / 5+spread per win / 1 per push (Man 23, Machine 10) — the rulebook
is still unsettled.

**Machine changes that go live this cycle:** `CAP_SWITCH_DATE = 2026-09-20`
flips `inseason_ratings.py` to the residual cap (the cap-artifact fix,
INSEASON_UPDATE.md amendment; backtested, same accuracy, better calibration).
Expect the Top 25 to jump on the Sunday/Tuesday refresh (OSU/Texas ~30,
Notre Dame ~29, Georgia ~27, South Carolina up to ~#15, cupcakes down 4–6) —
explain it on air once. The efficiency layer (`efficiency.py`, deserved
margins) is wired but OFF for the rating (`EFF_W = 1.0`, backtest neutral);
`deserved_margins()` feeds the receipts slide and the luck column in the
notes. Luck and line movement are INTERNAL (notes only, never slides).

**Deck template (make_episode_deck.py) as it stands:** 21 slides, headline
form (`SHORT` dict). Corey's feedback applied: ONE why-it-matters point per
number slide, THREE keys per team slide, one fact per COACH/QB/ROSTER cell
(`SLIDE_POINTS`, `SLIDE_KEYS`, `one_fact`). Receipts rows carry "deserved
XXX +n.n". No LINE MOVE readouts, no luck column. Game slides = game talk,
never betting talk (totals / line moves / "home dog" stay in the notes as
"(Internal: …)" asides); the machine line, fair odds, market line, score
call and honesty box STAY. Run `deck_lint.py` before every push (lists
lines and stat tokens on 2+ slides); Lucas still has to pick the slot map
for the three structural repeats (score call: number slide + closer; market
line: card + number + closer; game title).

**Notes format from Ep5 on (this is the big change):** NOTES, NOT A SCRIPT,
in Lucas's outline style — topic → the points → supporting facts indented
under each point. TWO files: `week4_ep5_boards.md` (receipts, W/L, Heisman
machine + Corey's five, Top 25 machine + Corey's, Hot Seat machine + Corey's
ten, superdog rules + picks) and `week4_ep5_games.md` (the five games: the
read in three lines, the number/call/honesty, each team's three keys +
reserve, Corey's call). Nothing repeats between them. EVERY KEY needs its
WHY (injuries / new personnel / scheme / underperformance) and its NUMBERS
from CFBD play-by-play — explosives per dropback (20+/30+), deep-throw rate,
yards per attempt vs per completion, third-and-long conversions, stuffs,
sacks, red zone — plus "what the opponent brings". The models are
`example_notes_boards.md` and `example_notes_games.md` (Ep4 rewritten that
way; the stat-package script is in the session history — re-derive from
`/plays` per team per week via `cfbd_client`). Board segments focus on
MOVERS: Top 25 risers/fallers with the game and the miss vs expectation, Hot
Seat = who moved into/out of the ten, Heisman = biggest index risers/fallers
with which input moved. Include the rating explainer (points vs an average
team; prior worth three games; Oregon-style worked example) and the seat
score formula (60% CBS + 40% odds of missing the bar).

**Corey's deck and the merge:** his file
(`1UpeONbbr4Wg26vIgMZWP5zaU8MrT-ziyZGOqDD5z8pg`) is edited in place each
week — READ ONLY for us, never edit it. The merged deck
(`EP4_MERGED_FILE_ID` in push_deck.py, shared with Corey as editor) is his
deck + our slides via `merge_deck.py --his-id <id> --ours decks\X.pptx`;
DO NOT build or push a merge until Lucas says so ON PODCAST DAY. The push
guard refuses if Corey was the last modifier — never `--force` without
asking. New Ep5 files: our own Slides file via `push_deck.py --new`, share
with `share_deck.py --file-id <id> --email credds19@gmail.com --role writer`
only when Lucas says. Merge only from Corey's CURRENT file (or from the
live merged file if he edited that) so his edits survive.

**Week 4 checklist:** 1) Sunday: grade Week 3 (finals vs the frozen lines;
receipts slide RECAP_ROWS + WEEK0_MISS running totals in make_episode_deck.py
— add Week 3 to the season total; superdog ledger; Corey's off-by column is
HIS to fill), note the residual-cap flip in the ratings. 2) Tue 7:30 AM
weekly refresh runs itself; `hot_seat_heisman.py --week 4 --refresh` after
updating MARKET/MARKET_DATE by hand (Covers 404s to automation). 3) Pick
the Week 4 card with Lucas (kickoff order, marquee last; superdogs FBS-vs-FBS
only), build GAMES/SHORT for Ep5 (WEEK, EPISODE = 5, 4 in make_episode_deck.py),
`edge_report.py --week 4 --view ml --publish`, `make_episode_deck.py`,
`deck_lint.py`, push to a NEW Ep5 Slides file. 4) Write the two outline notes
files as above. 5) Merge only on the day, on Lucas's word.

**Open items:** slot map for the three structural repeats on the deck; the
superdog rulebook (ours vs Corey's); confirm Corey's score-slide convention;
Heisman market must be updated by hand each week.
