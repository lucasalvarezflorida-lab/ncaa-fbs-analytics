# Handoff prompt — Man vs Machine, Ep4 / Week 3 (written Sun Sep 13, 2026)

Paste everything below the line into a new session.

---

Continue my "Man vs Machine — College Football Podcast" work (co-host Corey
Reddy). Project: `C:\Users\lucas\Fun Projects\Sports Data Analysis\ncaa-fbs-model`
(git repo lucasalvarezflorida-lab/ncaa-fbs-analytics). Read your memory file
`fpi-decomposition-project.md` and `podcast-deck-vs-notes.md` first, plus the
`cfb-pipeline` skill. Last commit: 9775281 (Ep4 built).

**Where we are (Sun 9/13):** Episode 4 / Week 3 is BUILT but not yet
recorded. Week 2 is graded (machine 40.5 vs market 48.0, closer in 4 of 5;
Michigan +5.5 lean won outright; stated leans 4–2; superdogs 0-for-4). The
machine re-solved on 100 rated games. AP Week 3 poll is in via a hand-typed
fallback (`_AP_FALLBACK`, week 3) because CFBD hadn't attached it;
`latest_rankings()` now skips AP-less weeks. Hot Seat and Heisman boards
refreshed (`hot_seat_heisman.py --week 3`; DK odds Sun 9/13). Deck:
`decks/2026_Week3_Episode4.pptx` (21 slides, headline-only template). Notes:
`week3_ep4_podcast.md` in slide order. Ep4 Google Slides file id
`1wjJAa0AL7t7sv2lPBOQUGEciNqLuctqZFMpds3yl6kw` (push_deck.py EP4_FILE_ID) —
in my Drive, SHARED with Corey as EDITOR (share_deck.py, Mon 9/14 - Drive permission list confirms writer) - the last_modifier guard runs before every push. Ep3's file is the aired record;
never push over it.

**Week 3 card, kickoff order, marquee last:** Houston at Texas Tech (Fri —
no total/ML posted yet, score call pending), SMU at Louisville, Mississippi
State at South Carolina, Florida at Auburn, LSU at Ole Miss. Three Monster
Under totals (58.5/58.5/59.5). Superdog = Sacramento State +26.5 vs NDSU
(caveat: 19-point machine/market gap; NDSU counts as FBS in CFBD 2026);
Giant Killer = Utah State +27.5 at #17 Utah. Frozen machine lines for
grading: TTU −13, LOU −1, SC −5, AUB −0.5, LSU −5.

**Rules in force:** slides = headline only ("Attack Poppinga early"; the
`SHORT` dict in make_episode_deck.py), all depth in the .md in slide order
with "## Slide N" headers; superdogs FBS-vs-FBS only; before any push to a
shared deck check `last_modifier` — if Corey touched it, ask me, never
`--force`; the Ep3 file never gets pushed again; ask which game closes if
unsure (I moved Ohio State–Texas last in Ep3).

**Before recording (likely Tue/Wed):** 1) `python edge_report.py --week 3
--view ml --publish` (Houston–Tech total should post); 2) update
MARKET/MARKET_DATE in hot_seat_heisman.py if DK moves, then `python
hot_seat_heisman.py --week 3 --refresh`; 3) `python refresh_all.py
--import-only` only if the sim needs the Tuesday games; 4) `python
make_episode_deck.py`; 5) `python push_deck.py --pptx
decks\2026_Week3_Episode4.pptx --file-id 1wjJAa0AL7t7sv2lPBOQUGEciNqLuctqZFMpds3yl6kw`;
6) sync the notes' numbers (Slide 4 table, Slide 21) and commit.

**Open items I may ask about:** (DONE 9/14) share the Ep4 file with Corey (editor, like
Ep3); the "man off by" column on the receipts slide is COREY'S - he types his
own calls into his deck (Week 1: 6/31/4/7/0), we do not generate it; the cap-artifact fix (cap the residual,
not the margin) for the postseason backtest.

---

## Update — Mon Sep 14, 12:35 PM MT (session 2)

- Ran the Monday pull (`edge_report.py --week 3 --view ml --publish`, 57 games
  priced). Houston at Texas Tech now has a total (Bovada 53.5; DK still none)
  and a moneyline (Tech -300 / Houston +270); DK spread walked to -7.5
  (first-seen -13.5, CLV +6). Score call filled in: **Texas Tech 33-20**.
  Other moves: LSU -3 (was -2.5), MSST-SC total 59.5 (was 58.5), Florida
  -2.5 unchanged with the Auburn gap now 8.8 pp. Machine lines unchanged, so
  the frozen grading lines stand: TTU -13, LOU -1, SC -5, AUB -0.5, LSU -5.
- Superdog boards refreshed: Sacramento State +27.5 vs NDSU (ML +2500),
  Giant Killer Utah State +28.5 at #17 Utah (ML +2000). Points = the spread
  at the pick, so re-read them off the pre-record pull.
- Deck rebuilt (21 slides) with the Houston/LSU/MSST headline text synced
  (SHORT + GAMES), pushed to the Ep4 Slides file. Notes synced slide by slide
  and a new **appendix** added: the machine line, market, total, win prob,
  score call and flags for every rated Week 3 game (57) in kickoff order,
  for any game Corey adds to his card.
- Corey's shared deck (his Week 2 file, edited Mon 12:33 PM MT) already holds
  the Ep4 Heisman and Top 25 slides plus his Week 2 Winners/Losers and his
  own superdog scoreboard: 3.5+ point dogs, 5 for a cover, 5 + spread for a
  win, 1 for a push, standing Man 11.5 / Machine 10. Our rule (win only,
  0-for-4, 0 points) differs; the Slide 21 notes flag it to settle on air.
- Heisman market (step 2): Covers pages 404 for automated fetch; MARKET /
  MARKET_DATE in hot_seat_heisman.py still reads DK Sun 9/13 - update by hand
  if the board moved, then `hot_seat_heisman.py --week 3 --refresh` and
  rebuild.
- Cap-artifact fix DONE (Mon 9/14): `inseason_ratings.ridge_update(cap_mode=
  "residual")` clips the residual, not the margin; backtested 2021-25 (same
  MAE, better out-of-sample calibration; INSEASON_UPDATE.md amendment,
  `cap_bias_check.py`). Date-gated: `CAP_SWITCH_DATE = 2026-09-20`, so the
  Ep4 card stays on the margin cap and the Week 4 cycle (Tue 9/22 refresh)
  runs the residual cap. Expect the Top 25 to jump then (OSU/Texas 30.0, ND
  28.8, UGA 27.3, South Carolina to #15; cupcakes fall 4-6). Week 4 grading
  lines come from the residual cap.
- Efficiency layer DONE (Mon 9/14): efficiency.py (deserved margin = 0.98 +
  0.887 net total PPA + 20.8 net success rate, fit 2021-24, R2 0.81, frozen in
  efficiency_model.json), backtest_efficiency.py. Verdict: blending it into
  the rating is NEUTRAL (never better than the actual margin out of sample),
  so EFF_W = 1.0 (off) in inseason_ratings.py; `--eff-w 0.5` re-runs the
  solve with it. Deserved margins are used as information: Slide 5 receipts
  and a Slide 1 luck column in the notes (Texas Tech +14.1, Texas +12.5,
  Oregon -7.5 through Wk 2). `inseason_ratings.deserved_margins()` returns
  them for 2026. INSEASON_UPDATE.md amendment (b) has the tables. Lucas asked
  (9/14) for the DECK, then reviewed: the LUCK column is INTERNAL (notes only,
  removed from the Top 25 slide the same day); the receipts
  rows carry "deserved XXX +n.n" on the line row, with "vs the deserved
  margins: machine 23.6" in the summary bar - these are now part of the
  frozen template. Nothing
  else open.
